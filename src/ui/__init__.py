"""Thin UI layer.

This package will contain Streamlit pages that:
- collect inputs
- start jobs (subprocess)
- poll job status
- render artifacts

Business logic should live in src/core or src/jobs handlers.
"""
